<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC 
    "-//Hibernate/Hibernate Configuration DTD 3.0//EN" 
    "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<!--
Local Event Management System RMI Server - Hibernate Configuration
Updated for Hibernate 6.x and JDK 21 compatibility
-->
<hibernate-configuration>
  <session-factory>
    <!-- Database connection settings -->
    <property name="hibernate.connection.driver_class">org.postgresql.Driver</property>
    <property name="hibernate.connection.url">jdbc:postgresql://localhost:5432/local_event_management_db</property>
    <property name="hibernate.connection.username">postgres</property>
    <property name="hibernate.connection.password">20223</property>
    
    <!-- SQL dialect for PostgreSQL -->
    <property name="hibernate.dialect">org.hibernate.dialect.PostgreSQLDialect</property>
    
    <!-- Echo all executed SQL to stdout -->
    <property name="hibernate.show_sql">true</property>
    <property name="hibernate.format_sql">true</property>
    <property name="hibernate.use_sql_comments">true</property>
    
    <!-- Drop and re-create the database schema on startup -->
    <!-- Options: validate | update | create | create-drop -->
    <property name="hibernate.hbm2ddl.auto">update</property>
    
    <!-- JDBC connection pool settings (using built-in) -->
    <property name="hibernate.connection.pool_size">10</property>
    
    <!-- Enable Hibernate's automatic session context management -->
    <property name="hibernate.current_session_context_class">thread</property>
    
    <!-- Disable the second-level cache  -->
    <property name="hibernate.cache.use_second_level_cache">false</property>
    <property name="hibernate.cache.use_query_cache">false</property>
    
    <!-- JDBC batch size for better performance -->
    <property name="hibernate.jdbc.batch_size">20</property>
    
    <!-- Enable statistics for monitoring -->
    <property name="hibernate.generate_statistics">false</property>
    
    <!-- Entity mappings -->
    <mapping class="model.User"/>
    <mapping class="model.Venue"/>
    <mapping class="model.Event"/>
    <mapping class="model.Booking"/>
    <mapping class="model.UserProfile"/>
    <mapping class="model.EventTag"/>
  </session-factory>
</hibernate-configuration>

